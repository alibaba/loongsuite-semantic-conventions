groups:
  - id: metric.gen_ai.workflow.duration
    type: metric
    metric_name: gen_ai.workflow.duration
    annotations:
      code_generation:
        metric_value_type: int
    brief: 'GenAI workflow invocation duration.'
    instrument: histogram
    unit: "ms"
    stability: development
    extends: metric_attributes.gen_ai
    attributes:
      - ref: gen_ai.workflow.name
        requirement_level: required
      - ref: error.type
        requirement_level:
          conditionally_required: "if the operation ended in an error"

  - id: metric.gen_ai.agent.duration
    type: metric
    metric_name: gen_ai.agent.duration
    annotations:
      code_generation:
        metric_value_type: int
    brief: 'GenAI agent invocation duration.'
    instrument: histogram
    unit: "ms"
    stability: development
    extends: metric_attributes.gen_ai
    attributes:
      - ref: gen_ai.agent.name
        requirement_level: required
      - ref: error.type
        requirement_level:
          conditionally_required: "if the operation ended in an error"

  - id: metric.gen_ai.tool.duration
    type: metric
    metric_name: gen_ai.tool.duration
    annotations:
      code_generation:
        metric_value_type: int
    brief: 'GenAI tool execution duration.'
    instrument: histogram
    unit: "ms"
    stability: development
    extends: metric_attributes.gen_ai
    attributes:
      - ref: gen_ai.tool.name
        requirement_level: required
      - ref: error.type
        requirement_level:
          conditionally_required: "if the operation ended in an error"
